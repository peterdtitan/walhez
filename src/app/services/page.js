import React from 'react'

export const metadata = {
  title: "Services",
  description: "Browse Walhez Group service offerings across construction support and equipment operations.",
};

export default function page(){
  return(
    <div>
        Services Page
    </div>
  )
}
